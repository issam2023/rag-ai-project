FastAPI is a Python framework for building web APIs.
A machine learning or RAG system can expose endpoints using FastAPI.
For example, a /query endpoint can accept a user question and a /health endpoint can report whether the service is running.
